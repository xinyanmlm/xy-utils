[project]
name = "xy-utils"
version = "0.1.0"
description = "xinyan的工具包，code with deepseek."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mcp>=1.27.1",
    "sqlite-utils>=3.39",
]
